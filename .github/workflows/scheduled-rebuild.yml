name: Scheduled rebuild from Airtable

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch: {}

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: |
          curl -fsS -X POST "https://api.vercel.com/v1/integrations/deploy/prj_X9SCIAjBd1eFjfRXhYrGZ3iPQPYM/HCf3Mvnepw"
